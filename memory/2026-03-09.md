# 2026-03-09

## SwiftUI lesson saved

- Learned and validated a fix for iPhone top/bottom black bars in SwiftUI prototypes.
- Standard rule going forward:
  - Add `UILaunchScreen` empty dictionary to `Info.plist`.
  - Rebuild and reset simulator if needed.
- This is now documented in `TOOLS.md` under "SwiftUI / iPhone fullscreen fix (important)".
